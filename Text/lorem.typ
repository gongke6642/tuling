#set text(
  size:10pt,
  
)
#set page(
  paper:"a5",
  margin:(x:1.8cm,y:1.5cm),
)
#set par(
  justify: true,
  leading: 0.52em,
)

= 假文
创建假文

此函数生成类似拉丁语的 Lorem Ipsum 假文，有着给定的字数。 函数生成的字符串始终是相同但随机生成的。 假文没有对应的语义，只是用作占位符来测试你的布局。
= 例
#image("5.png")
= 参数
#image("6.png")
= 单词
以单词为单位的假文长度。