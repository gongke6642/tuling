#set text(
  size:10pt,
  
)
#set page(
  paper:"a5",
  margin:(x:1.8cm,y:1.5cm),
)
#set par(
  justify: true,
  leading: 0.52em,
)
= 重音
在附基上添加重音符号。
= 例
#image("7.png")
= 参数
#image("8.png")
= 基数
添加重音符号的基数，可以由多个字母组成。
= 重音
可添加于附基的重音符号。

支持的重音符号如下：
#image("9.png")
= 大小
重音的大小，相对于底座的宽度。

默认：auto