#set text(
  size:10pt,
  
)
#set page(
  paper:"a5",
  margin:(x:1.8cm,y:1.5cm),
)
#set par(
  justify: true,
  leading: 0.52em,
)
= 类
强制使用特定数学类。

这对于将某些符号视为不同的类别非常有用，例如，使符号的行为类似于关系。符号的类定义其布局方式，包括其周围的间距，以及默认情况下其脚本的附加方式。请注意，后者始终可以使用限制和脚本覆盖。
= 例
#image("19.png")
= 参数
#image("20.png")
= 类
应用于内容的类。
#image("21.png")
= 内容
类所应用的内容。